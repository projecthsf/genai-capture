# Plugin-only override of the reused app's application.properties. The desktop app
# ships its own (app.name=GenAI Capture); inside the JetBrains plugin the brand is
# "Desktop Capture", so the shared engine (overlay, messages) shows the plugin name.
app.name=Desktop Capture
app.version=1.0.0
